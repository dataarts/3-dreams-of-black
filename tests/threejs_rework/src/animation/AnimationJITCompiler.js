/*
 * Animation JIT Compiler. Compiles an animation into matrices, in a specified fps.
 */

THREE.AnimationJITCompiler = (function() {
	
	var that = {};
	
	that.compile = function( data, fps ) {
		
		// todo: compile keys into matrices		
	}
	
	return that;
}());

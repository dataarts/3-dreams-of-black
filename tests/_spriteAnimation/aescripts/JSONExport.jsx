#include CompParser.jsx#include LayerExportData.jsx#include JSONWriter.jsx#include ShellWriter.jsx#include Util.jsx#include Log.jsxExportSettings = {    floatPrecision: 3,    texFolder: "",    samplingRate: 0.5, // in sec       widthThreshold: 500};function main() {    var comp = app.project.activeItem;       Log.info("Current comp is " + comp.name + ", duration: " + comp.workAreaDuration);    ExportSettings.duration = comp.workAreaDuration;        var parser = new CompParser(comp);        var file = Util.selectFile();        var json = new JSONWriter(file);       json.write(parser.createExportObject(), Util.cleanName(comp.name));        var shell = new ShellWriter(file);    shell.write(parser.flatLayers);}main();
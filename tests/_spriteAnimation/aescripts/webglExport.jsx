#script "Export active composition to WebGL"#include webglUtil.jsx#include webglJSONFormating.jsxvar comp = app.project.activeItem;var texFolder = "export2/";var samplingRate = 0.5; // in secvar layers = {};var totalTime = comp.workAreaDuration;var precision = 3;// ### 2. Get the export filevar efile = selectFile();efile.writeln ( JSF.startDocument("animation") );efile.writeln ( "samplingRate: %0,".format(samplingRate) );efile.writeln ( "totalTime: %0,".format(totalTime) );JSF.exportComposition(comp, true);efile.writeln ( JSF.endDocument() );efile.close();logClose ();